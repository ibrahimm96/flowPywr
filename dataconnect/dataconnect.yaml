specVersion: "v1beta"
serviceId: "flowpywrapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "flowpywrapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
